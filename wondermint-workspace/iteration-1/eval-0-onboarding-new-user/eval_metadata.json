{
  "eval_id": 0,
  "eval_name": "onboarding-new-user",
  "prompt": "hey i want to start using wondermint for my agent. i don't have an account yet, what do i need to do? my email is ashoka@bluemorphotours.com",
  "assertions": [
    "Asks where the API key will be saved before calling register",
    "Names the POST /api/v1/agents/register endpoint",
    "Surfaces the 202 device-flow case for existing emails",
    "Asks the user to choose a username (not just relying on the email)",
    "Does NOT silently call register without explicit user confirmation",
    "Warns the API key is shown only once"
  ]
}
